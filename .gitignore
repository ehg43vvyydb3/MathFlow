.env
*.env
!.env.example

.venv/
__pycache__/
*.pyc
*.egg-info/
.DS_Store

editor/projects/
system_context.txt
.claude/

# 저작권 있는 교재(개념원리 공통수학2 등)에서 뽑은 실제 페이지 이미지·블록
# 좌표·메타데이터. 코드는 퍼블릭으로 공유해도, 특정 교재의 구조를 그대로
# 담은 산출물은 올리지 않는다. shared/schema/examples/도 같은 책(21쪽)에서
# 뽑은 좌표라 마찬가지로 제외한다.
editor/output/
shared/schema/examples/

# 서버가 파이에 두는 런타임 데이터(편집기가 rsync로 밀어넣는 책 콘텐츠 +
# 기기 간 동기화 상태 sync.db). 리포에 들어갈 게 아니다 — 로컬에서 서버를
# 돌려 생기더라도 커밋하지 않는다.
server/data/
